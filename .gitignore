# OS
.DS_Store
Thumbs.db
desktop.ini
# Editors
*.swp
*.swo
*~
.vscode/
.idea/
# Local agent settings (per-machine, not shared)
.claude/settings.local.json
# Secrets — never commit
.env
*.pem
*.key
*.p12
*.pfx

# Minted lab certificates. The gen scripts rebuild them on demand and the
# committed copies would expire, so history keeps the recipe, not the output.
certs/
